/** Horizontal padding for full-bleed sections (mobile-safe). */
export const sectionX =
  "px-5 sm:px-8 lg:px-10 xl:px-12 2xl:px-14";

/** Primary page content width — editorial portfolio scale. */
export const container =
  "mx-auto w-full max-w-7xl 2xl:max-w-[88rem]";

/** Floating nav — slightly narrower than content for visual balance. */
export const navShell =
  "w-full max-w-7xl 2xl:max-w-[80rem]";

/** Hero copy block — wide but not full-bleed on ultra-wide. */
export const heroInner =
  "mx-auto w-full max-w-7xl 2xl:max-w-[84rem]";

/** Long-form project body — readable line length with breathing room. */
export const prose =
  "mx-auto w-full max-w-[46rem] xl:max-w-[48rem] 2xl:max-w-[52rem]";

/** Project detail hero image — matches main container. */
export const projectHero =
  "mx-auto w-full max-w-7xl 2xl:max-w-[88rem]";

/** Project gallery / content images — same width as hero for consistent scale. */
export const projectGallery = projectHero;

/** Three-column card grids (projects, pillars). */
export const cardGrid =
  "grid gap-12 sm:gap-14 lg:grid-cols-3 lg:gap-10 xl:gap-12";
